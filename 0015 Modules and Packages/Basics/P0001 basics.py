import string_module  # import module like this


print(__name__)  # __main__ because this is your main file, and because this is the main file we are running
strReverse = string_module.reverseString('hallo')
print(strReverse)
